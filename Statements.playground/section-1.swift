// Playground - noun: a place where people can play

import UIKit

var str = "Hello, playground"
// if statements are the same as in javascript

let amount = 500
var price = 400

if amount > price {
    println("The amount is more than the price")
} else {
    println("The price is more than the amount")
}
